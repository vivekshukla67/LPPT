import { INotify } from "./INotify";
import { ToastTypeEnum } from "../../Enums/Enums";

export class NotificationDrawer implements INotify{
    
    Show(title: string, description: string, type: ToastTypeEnum){
        
    }
}